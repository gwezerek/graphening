/**
*
* Filter
*
**/

'use strict';

var d3 = require( 'd3' );
var utils = require( '../utils' );

function init( loadedJSON ) {
	var data = loadedJSON;
}

console.log('test');

module.exports = init;
